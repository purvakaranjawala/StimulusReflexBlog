<p id="notice"><%= notice %></p>

<%#Reflex via  Nothing Morph %>

<button data-reflex="click->Counter#increment_nothing_morph">Increment Counter</button>
​
The counter currently reads:
<span id="increment_nothing_morph">
  <%= Rails.cache.fetch("increment_nothing_morph", raw: true) {0} %>
</span>
<br /><br /><br />

<%#Reflex via  Page Morph %>
<a href="#"
  data-reflex="click->Counter#increment"
  data-step="1"
  data-count="<%= @count.to_i %>">
  Increment <%= @count.to_i %>
</a>
<br/><br/><br/><br/>
<div>
  <%= render "players/form", player: @player %>
</div>
<br>
<%#Reflex via  Selector Morph %>
<div class="p-10">
  <h1 class="mb-4 text-4xl font-black">Players</h1>
  <div data-controller="player-search">
    <%= form_with(method: :get) do |form| %>
      <%= form.label :query, "Search by name:" %>
      <%= form.text_field :query, value: params[:query], data: { controller: "player-search", action: "input->player-search#search" }, autocomplete: "off" %>
      <%#= form.submit 'Search', name: nil %>
    <% end %>
    <br>
    <div id="reset-link">
      <%= render "reset_link", query: params[:query] %>
    </div>
  </div><br>
  <div data-controller="scroll" style="height: 3000px">
    <table class="w-full text-sm text-left text-dark-500 dark:text-dark-400">
      <thead class="text-lg text-dark-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-dark-400">
        <tr>
          <th></th>
          <th scope="col" class="py-3 px-6">Name</th>
          <th scope="col" class="py-3 px-6">Age</th>
          <th scope="col" class="py-3 px-6">Contact</th>
          <th scope="col" class="py-3 px-6">Bio</th>
          <th scope="col" class="py-3 px-6">Joined At</th>
          <th colspan="3">Actions</th>
        </tr>
      </thead>

      <%= render "players", players: @players %>
    </table>
  </div>
</div>
<br>
